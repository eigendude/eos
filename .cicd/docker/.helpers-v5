function execute() {
    ${TRAVIS:-false} && echo -en "travis_fold:start:$(echo $@)\r"
    echo "--- Executing: $@"
    "$@"
    echo " - Execution complete"
    ${TRAVIS:-false} && echo -en "travis_fold:end:$(echo $@)\r"
}