function execute() {
    ${TRAVIS:-false} && echo -en "travis_fold:start:test\r" || echo "--- Executing: $@"
    "$@"
    ${TRAVIS:-false} && echo -en "travis_fold:end:test\r" || echo " - Execution complete"
}